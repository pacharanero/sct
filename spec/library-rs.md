# sct as a Rust library

This original Rust-only design has been superseded by [`sdk.md`](sdk.md), which reflects the library surface already shipped and roadmaps the Rust SDK, Python bindings, WebAssembly build, documentation structure, and licensing decisions together.

The stable file remains as a compatibility pointer for existing links.
